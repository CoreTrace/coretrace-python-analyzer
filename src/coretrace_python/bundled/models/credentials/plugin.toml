name = "credential-models"
version = "1.0.0"
plugin_api = ">=1,<2"
requires = []
provides = ["model.credential-sources", "model.credential-sinks"]

[entrypoint]
module = "credential_models"
class = "CredentialModels"
