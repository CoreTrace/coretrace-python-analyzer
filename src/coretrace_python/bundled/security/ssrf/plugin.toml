name = "ssrf"
version = "1.0.0"
plugin_api = ">=1,<2"
requires = ["taint.flows", "findings.refutation", "ir.ssa", "abstract.module_strings"]
provides = ["vulnerability.ssrf"]

[entrypoint]
module = "ssrf"
class = "SsrfPlugin"
