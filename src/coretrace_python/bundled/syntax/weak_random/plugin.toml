name = "weak-random"
version = "1.0.0"
plugin_api = ">=1,<2"
requires = ["semantic.scopes", "semantic.symbols"]
provides = ["vulnerability.weak-random"]

[entrypoint]
module = "weak_random"
class = "WeakRandomPlugin"
